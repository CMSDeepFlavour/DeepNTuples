Contains packages to be checked out locally (not for mass production)
Devel-tools or XCode on Mac is required
Anaconda is used as package manager

To install the full package, run setupEnv.sh within this directory
Don't forget to activate the environment afterwards.
